services:
  app:
    build: .
    ports:
      - "8001:8000"
    environment:
      - SECRET_KEY=${SECRET_KEY:-change-me-in-production}
      - DB_PATH=/tmp/prelegal.db
      - OPENROUTER_API_KEY=${OPENROUTER_API_KEY}
